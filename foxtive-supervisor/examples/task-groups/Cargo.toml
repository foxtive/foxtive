[package]
name = "task-groups"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
foxtive-supervisor = { path = "../..", features = ["cron"] }
tokio = { version = "1", features = ["full", "signal"] }
async-trait = "0.1"
tracing = "0.1"
tracing-subscriber = "0.3"
